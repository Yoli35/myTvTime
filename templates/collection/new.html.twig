{% extends 'base.html.twig' %}

{% block title %}myTvTime::{{ 'My Collections'|trans }}::{{ collection.title }}{% endblock %}

{% block body %}
    {{ include('blocks/_pageHeader.html.twig') }}
    <div class="container">
        <div class="my-collections-page">
            {{ include('blocks/_nav.html.twig', {parent: {page: 'My Collections', url: path('app_collection')}, thisPage: 'New collection'|trans}) }}

            <h1>{{ 'New collection'|trans }}</h1>

            <main>
                {{ include('blocks/collection/_form.html.twig') }}
            </main>

        </div>
        <footer>
            {{ include('blocks/_pageFooter.html.twig') }}
        </footer>
    </div>
{% endblock %}
{% block morejs %}
{% endblock %}
