{% extends 'base.html.twig' %}

{% block title %}myTvTime ▶ ︎{{ 'My events'|trans }} ▶ ︎{{ 'Edit event'|trans }}{% endblock %}
{% block description %}{{ 'List of all my events'|trans }}{% endblock %}
{% block keywords %}{{ 'event'|trans }}{% endblock %}

{% block body %}
    {{ include('blocks/_mainMenu.html.twig') }}
    <div class="container">
        <div class="my-events">
            <div class="header">
                <h1>{{ 'Edit event'|trans }}</h1>
                <div class="backdrop"></div>
            </div>

            {{ include('blocks/_nav.html.twig', {thisPage:('New event'|trans), parent: {url: path('app_event'), page: 'My events'|trans}}) }}

            <main>
                {{ include('blocks/event/_form.html.twig') }}
            </main>

        </div>
        {{ include('blocks/_pageFooter.html.twig') }}
    </div>
    <div id="countdown-values" style="display: none">
        {
        "route": "event_edit",
        "save_button": "{{ "Update the event"|trans }}"
        }
    </div>
{% endblock %}
