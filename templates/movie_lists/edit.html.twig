{% extends 'base.html.twig' %}

{% block title %}myTvTime ▶ ︎{{ 'My lists'|trans }} ▶ ︎{{ 'Edit list'|trans }} ▶ ︎{{ title }}{% endblock %}

{% block body %}
    {{ include('blocks/_mainMenu.html.twig') }}
    <div class="container">
        <div class="movie-list-page">
            {{ include('blocks/_nav.html.twig') }}

            <h1>{{ 'Edit list'|trans }}</h1>

            <main>
                {{ include('blocks/movie_list/_form.html.twig') }}
            </main>

        </div>
        <footer>
            {{ include('blocks/_pageFooter.html.twig') }}
        </footer>
    </div>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script>
        window.addEventListener("DOMContentLoaded", () => {
            document.querySelector("#movie_list_save").innerHTML = "{{ "Update the list"|trans }}";
        })
    </script>
{% endblock %}
