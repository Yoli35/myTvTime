{% extends 'base.html.twig' %}

{% block title %}myTvTime ▶ ︎{{ 'My lists'|trans }} ▶ ︎{{ 'New list'|trans }}{% endblock %}

{% block body %}
    {{ include('blocks/_mainMenu.html.twig') }}
    <div class="container">
        <div class="movie-list-page">
            {{ include('blocks/_nav.html.twig') }}

            <h1>{{ 'New list'|trans }}</h1>

            <main>
                {{ include('blocks/movie_list/_form.html.twig') }}
            </main>

        </div>
        <footer>
            {{ include('blocks/_pageFooter.html.twig') }}
        </footer>
    </div>
{% endblock %}
